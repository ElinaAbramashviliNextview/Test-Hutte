<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ClosedWonFinalizingOpp_ClientRefNum</fullName>
    <active>false</active>
    <description>Clinet Reference Number must be filled in order to be able to move the Opportunity to Finalising or Closed Stages</description>
    <errorConditionFormula>AND(
 ISBLANK(ClientReferenceNumber__c ),
	Client_reference_Nr_NOT_REQUIRED__c=FALSE,  
OR( 
(ISPICKVAL(StageName, &quot;Closed Won&quot;)), 
(ISPICKVAL(StageName, &quot;Finalizing&quot;))  
))</errorConditionFormula>
    <errorDisplayField>ClientReferenceNumber__c</errorDisplayField>
    <errorMessage>Client Reference Number needs to be filled before changing Stage to Closed Won or Finalizing.</errorMessage>
</ValidationRule>
