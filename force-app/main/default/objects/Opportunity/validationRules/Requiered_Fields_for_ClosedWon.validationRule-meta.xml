<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Requiered_Fields_for_ClosedWon</fullName>
    <active>true</active>
    <description>Required Fields for Closed Won:
- Order Due Date
- Realization Start Date
- Realization End Date
- Primary Contact
- On-site Contact</description>
    <errorConditionFormula>AND(
	ISPICKVAL(StageName, &quot;Closed Won&quot;),
	OR(
		ISNULL(OrderDueDate__c),
		ISNULL(RealizationStartDate__c),
		ISNULL(RealizationEndDate__c),
                ISNULL(PrimaryContact__c),
                ISNULL(OnSiteContactId__c)
	)
)</errorConditionFormula>
    <errorMessage>Required Fields for Closed Won:
- Order Due Date
- Realization Start Date
- Realization End Date
- Primary Contact
- On-site Contact</errorMessage>
</ValidationRule>
