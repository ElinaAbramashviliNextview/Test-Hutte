<apex:page controller="GoogleCalendarOAuthController">
<apex:form >
    <apex:pageblock >
        <apex:commandbutton action="{!CalendarAuth}" value="Google Calendar Authentication"></apex:commandbutton>
        <apex:outputPanel rendered="{!IF(code != null, true, false)}">
            <apex:commandbutton action="{!AccessToken}" value="Get Token and Save"></apex:commandbutton>
        </apex:outputPanel>
    </apex:pageblock>
</apex:form>
</apex:page>